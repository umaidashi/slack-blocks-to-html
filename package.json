{
  "name": "slack-blocks-to-html",
  "version": "1.0.1",
  "description": "my first npm package!",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "umaidashi.",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.1.3"
  }
}
